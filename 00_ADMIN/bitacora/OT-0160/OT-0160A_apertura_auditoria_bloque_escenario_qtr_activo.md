# OT-0160A — Apertura auditoría bloque Escenario Q-Tr activo

## Objetivo

Seleccionar y auditar el bloque documental `## 5. Escenario Q-Tr activo — control de trazabilidad` como siguiente candidato del expediente hidrológico mínimo.

## Antecedente

Los bloques `## 1. Identificación`, `## 2. Parámetros hidrológicos base`, `## 3. Tiempo de concentración y roles Tc` y `## 4. Volumen de referencia` ya fueron delegados, validados y estabilizados.

## Candidato auditado

`## 5. Escenario Q-Tr activo — control de trazabilidad`

## Alcance

Esta OT solo audita y clasifica el bloque candidato.

No implementa helper.

No modifica `ComparadorMultiMetodo.jsx`.

No sustituye `textoExpediente`.

## Precaución técnica

El bloque puede contener información sensible de trazabilidad operativa asociada al escenario Q-Tr activo, estado de publicación, periodo de retorno, fuente y coherencia de caudal.

## Restricciones

No se modifica:

- `ComparadorMultiMetodo.jsx`;
- `textoExpediente`;
- botón de copiado;
- portapapeles;
- Q-5;
- Método Racional;
- diagnóstico Q(t);
- validadores finales;
- motor hidrológico.
